services:
  - type: web
    name: trang-mockup-dashboard
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: META_ACCESS_TOKEN
        sync: false
      - key: META_AD_ACCOUNT_ID
        sync: false
      - key: PORT
        value: 3000
